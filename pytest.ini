[pytest]
# Lets `import pipeline` resolve from the project root when pytest is run
# from anywhere (tests/ has no __init__.py, so pytest wouldn't otherwise put
# the root on sys.path).
pythonpath = .
testpaths = tests
